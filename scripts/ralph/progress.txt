# Ralph Progress Log
Started: 2026-03-15
---

## Codebase Patterns
- ESM `.js` extension on ALL imports: `import { ok, err } from '../contracts/errors.js'`
- `execSync` from `node:child_process` for git commands — wrap in try/catch returning Result<string>
- Mock `node:child_process` with `vi.mock('node:child_process', () => ({ execSync: vi.fn(() => '') }))` in tests that touch git
- `Result<T, CliError>` for all fallible functions — never `throw`
- `ERROR_CODES.*` constants in `src/contracts/errors.ts` — add new codes here, never inline strings
- `ok()` / `err()` helpers — never raw `{ ok: true, value }` objects
- Named exports only — no `export default`
- Audit logging FIRST — log intent before writing files
- All user strings via `I18nResolver.t()` — never hardcoded text
- Factory function pattern for test setup — `const opts = () => ({...})`
- Test imports from implementation files — not from barrel `index.ts`
- @clack/prompts API: `spinner.stop()` takes 1 argument; use `log.success()` / `log.warn()` for display
- Layer dependency order: contracts <- foundation <- engine <- commands <- cli
- Vitest 4.x for testing, mkdtemp for temp dirs, afterEach cleanup
- TypeScript strict mode, tsdown for build
- Node.js minimum 20.x, 22.x LTS recommended
---

## Pre-existing completed stories (Epic 1 + Story 2.1)

Stories US-001 through US-007 were completed before Ralph loop started.
All 157 tests passing. TypeScript compiles with zero errors.

---

## 2026-03-15 - US-008
- Implemented constitution-enforcer.ts with: extractPrinciples (parse constitution into named sections/rules), validateOutput (detect prohibition-keyword violations), formatViolationWarning (expert vs beginner mode), resolveConstitutionPath (returns path if constitution exists, undefined otherwise)
- Added CONSTITUTION_VIOLATION error code to contracts/errors.ts
- Added user consent confirmation before constitution edits in handler.ts (NFR-25)
- Added i18n keys for consent prompt and violation warnings in EN and PT-BR locales
- Updated existing constitution handler tests and integration test to provide consent `true` first
- Added 24 new unit tests in test/unit/engine/constitution-enforcer.test.ts
- Files changed: src/engine/constitution-enforcer.ts (new), src/contracts/errors.ts, src/commands/constitution/handler.ts, locales/en.yaml, locales/pt-br.yaml, test/unit/engine/constitution-enforcer.test.ts (new), test/unit/commands/constitution.test.ts, test/integration/pipeline/constitution-flow.test.ts
- **Learnings for future iterations:**
  - Prohibition keywords for pattern matching: "no ", "never ", "must not ", "do not ", "don't ", "prohibited", "forbidden", "disallowed", "avoid ", "ban ", "cannot ", "can't "
  - validateOutput does exact substring match (case-insensitive) — tests must use exact prohibited phrase, not morphological variants (e.g., "expose" not "exposes")
  - Integration tests that mock clack.confirm must account for ALL confirm calls in order — adding a new confirm shifts indices
  - `resolveConstitutionPath` returns the absolute path string (for injection into TaskDispatchPayload.constitutionPath)
---

## 2026-03-15 - US-009
- Implemented constitution-versioner.ts with: diffConstitutionPrinciples (diff two constitution versions into PrincipleChange[]), scanDownstreamArtifacts (scan .buildpact/specs/ and .buildpact/plans/ for files referencing changed principle names), buildChecklistContent (generate markdown checklist with What Changed / Reason / Downstream Artifacts sections), writeUpdateChecklist (write to .buildpact/constitution_update_checklist.md)
- Extended constitution handler.ts edit flow: captures old content before edit, asks user for reason (clack.text), calls versioner after save, writes checklist, logs to audit with both files, shows log.success
- Added i18n keys: cli.constitution.reason_prompt, cli.constitution.reason_placeholder, cli.constitution.checklist_generated in both EN and PT-BR locales
- Files changed: src/engine/constitution-versioner.ts (new), src/commands/constitution/handler.ts, locales/en.yaml, locales/pt-br.yaml, test/unit/engine/constitution-versioner.test.ts (new), test/unit/commands/constitution.test.ts
- 211 tests passing, zero typecheck errors
- **Learnings for future iterations:**
  - diffConstitutionPrinciples relies on extractPrinciples — "Version History" and "Immutable Principles" headings are excluded from principle extraction, so they won't show as changes
  - scanDownstreamArtifacts silently skips missing specs/plans directories — safe to call even on fresh projects
  - When adding a new clack.text prompt inside an existing edit flow, update ALL tests that mock clack.text in that path to add an extra mockResolvedValueOnce for the new prompt
  - writeUpdateChecklist uses mkdir({ recursive: true }) so it's safe even if .buildpact dir doesn't exist
---

## 2026-03-15 - US-010
- Implemented src/commands/quick/handler.ts with: inferCommitType (keyword-based conventional-commit type detection), buildQuickSpec (generates minimal spec markdown), runGit (wraps execSync for git add/commit), readLanguage (reused from constitution pattern)
- Quick flow: description from CLI args or clack.text prompt → resolveConstitutionPath → buildTaskPayload (type: 'quick') → write .buildpact/specs/<slug>/quick-spec.md → git add + git commit with type(quick): description format
- Updated src/commands/quick/index.ts to re-export from handler.ts
- Added 9 i18n keys in locales/en.yaml and locales/pt-br.yaml under cli.quick.*
- Added 15 unit tests in test/unit/commands/quick.test.ts (inferCommitType x6, handler x9)
- 226 tests passing, zero typecheck errors
- **Learnings for future iterations:**
  - `execSync` returns stdout as string when encoding: 'utf-8' is set in options — use stdio: ['pipe','pipe','pipe'] to capture stderr too
  - git commit message passed as JSON.stringify(message) to handle special chars safely in shell invocation
  - Mock `node:child_process` with full module mock (not spy) to reliably intercept execSync — spyOn fails for ESM
  - `inferCommitType` is exported for direct unit testing — pure function with no side effects
  - `resolveConstitutionPath` returns `string | undefined` — use undefined check before spreading into payload params
---

## 2026-03-15 - US-011
- Added `--discuss` mode to `/bp:quick` handler: `gatherDiscussContext(i18n)` iterates 4 questions (scope, risk, done, constraints) using `clack.select` with numbered options. If user selects 'other', a `clack.text` follow-up is shown.
- Modified `buildQuickSpec` to accept optional `answers?: DiscussAnswer[]` and append "Context from Discussion" section to spec.
- Exported `DiscussAnswer` interface and `gatherDiscussContext` function from handler.ts.
- Added 36 i18n keys under `cli.quick.discuss_*` in both EN and PT-BR locales.
- Added 6 new tests in `describe('quick handler --discuss mode', ...)` — spec generation, answer text reflection, 'other' free-text, cancel, 4-question count, execution flow.
- Files changed: src/commands/quick/handler.ts, locales/en.yaml, locales/pt-br.yaml, test/unit/commands/quick.test.ts
- 232 tests passing, zero typecheck errors
- **Learnings for future iterations:**
  - `vi.clearAllMocks()` clears call history but NOT implementations in Vitest v4 — use `mockImplementation` (not `mockResolvedValueOnce`) for reliable cross-test mock setups
  - Pattern for sequenced mock responses: `let idx = 0; vi.mocked(fn).mockImplementation(async () => responses[idx++] ?? fallback)` — immune to clearAllMocks queue-clearing
  - Always explicitly call `vi.mocked(clack.isCancel).mockImplementation(() => false)` in `beforeEach` for tests involving `gatherDiscussContext` — factory `() => false` may not survive clearAllMocks in all Vitest versions
  - `clack.select` must be added to the `vi.mock('@clack/prompts')` factory whenever handler uses it
  - Use `as const` on question arrays for type safety, but be aware `.find()` predicates comparing string literals to string values work fine at runtime
---

## 2026-03-15 - US-012
- Added 5 exported pure functions to handler.ts: `buildFullPlan` (generates 2-task plan.md with AC coverage + risk sections), `validatePlanCompleteness` (Perspective 1: checks spec ACs have keywords in plan), `validatePlanFeasibility` (Perspective 2: checks plan structure), `verifyAgainstSpec` (post-execution AC verification), `buildFixPlan` (targeted fix plan for failed ACs)
- Added `--full` flag to handler.run(): generates plan → 2-perspective validation → clack.confirm risk prompt → writes plan.md → verifyAgainstSpec → writes fix-plan.md if ACs fail
- Added `modeSuffix` optional param to `buildQuickSpec` so --full mode shows "quick (full)" in spec Mode label
- Added `confirm: vi.fn()` to clack mock in test file
- Added 9 i18n keys in en.yaml and pt-br.yaml under `cli.quick.full_*`
- Added 16 new tests: unit tests for all 5 pure functions + 5 handler integration tests for --full mode
- Files changed: src/commands/quick/handler.ts, locales/en.yaml, locales/pt-br.yaml, test/unit/commands/quick.test.ts
- 248 tests passing, zero typecheck errors
- **Learnings for future iterations:**
  - `TaskDispatchPayload.type` is a strict union — use existing members only (no `'quick-fix'`); `'quick'` works for fix payloads too
  - `clack.confirm` must be added to `vi.mock('@clack/prompts')` factory when handler uses it — unlike `select`/`text`, it returns boolean not string
  - `vi.mocked(clack.confirm).mockResolvedValue(true)` in `beforeEach` sets the default "proceed" answer for all full-mode tests
  - Plan verification uses keyword matching on AC text — short words (<5 chars) are filtered, so ACs with only short words will always pass (non-issue for real specs)
  - `buildFullPlan` AC coverage section always covers the 3 built-in quick-spec ACs, so normal --full runs produce no fix-plan
---
